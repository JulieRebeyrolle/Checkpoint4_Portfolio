<form method="post" action="{{ path('admin_portfolio_delete', {'id': portfolio.id}) }}" onsubmit="return confirm('Sur ?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ portfolio.id) }}">
    <div class="text-right mb-5">
        <button class="btn btn-danger">Effacer</button>
    </div>
</form>
