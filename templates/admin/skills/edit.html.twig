{% extends 'adminbase.html.twig' %}

{% block title %}{{ parent() }} Modifier une compétence{% endblock %}

{% block body %}
    <div class="content-container container">
        <a href="{{ path('admin_skills_index') }}" class="btn btn-primary mt-5">Retour à la liste</a>
        <h1 class="font-weight-bold text-dark text-center p3-5 pb-2">Modifier une compétence</h1>
        <div class="content-container container">
            {{ include('admin/skills/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
            {{ include('admin/skills/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}
