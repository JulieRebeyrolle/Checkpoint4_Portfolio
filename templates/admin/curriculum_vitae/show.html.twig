{% extends 'base.html.twig' %}

{% block title %}CurriculumVitae{% endblock %}

{% block body %}
    <h1>CurriculumVitae</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ curriculum_vitae.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ curriculum_vitae.title }}</td>
            </tr>
            <tr>
                <th>Place</th>
                <td>{{ curriculum_vitae.place }}</td>
            </tr>
            <tr>
                <th>StartingDate</th>
                <td>{{ curriculum_vitae.startingDate ? curriculum_vitae.startingDate|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>EndingDate</th>
                <td>{{ curriculum_vitae.endingDate ? curriculum_vitae.endingDate|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Resume</th>
                <td>{{ curriculum_vitae.resume }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('curriculum_vitae_index') }}">back to list</a>

    <a href="{{ path('curriculum_vitae_edit', {'id': curriculum_vitae.id}) }}">edit</a>

    {{ include('curriculum_vitae/_delete_form.html.twig') }}
{% endblock %}
